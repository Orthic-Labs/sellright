{
  "name": "sellright",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @sellright/api test",
    "verify": "pnpm -r build && pnpm -r typecheck && pnpm --filter @sellright/api test",
    "dev": "pnpm --filter @sellright/api dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
