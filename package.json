{
  "name": "sellright",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @sellright/api dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
