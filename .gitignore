node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
*.log
/data/
/.cache/
.commandcode/

# audit reports (local baseline diffing)
.audit/
.agent/

# editor/tool backup artifacts — never commit these
*.bak
*.backup
*.old
*.orig
*.tmp

# Per-deployment runtime state (assets, catalog/manifest) — never committed.
var/

# Local pnpm content-addressable store — never committed; large and per-machine.
.pnpm-store/

SR outside audit.md

# Private deployment and migration material must stay outside this public repo.
SECURITY-FIX-XSS.md
deployment-private/
migration-private/

# Cortex repo map: generated graph/caches/logs stay out; the authored config is repo content.
.agent/*
!.agent/config.json
