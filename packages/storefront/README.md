# @sellright/storefront — Qwik SSR storefront

Qwik (LOCKED — best for ecom). Reads the static catalog manifest + SSE
invalidation for browse; calls the typed `hc` API client for cart/checkout/account.

The current Qwik storefront is migrated in here when the API is ready
(M2 onward). Placeholder for now. See ../../docs/BUILD-PLAN-RH-v1.md.
