# @sellright/admin — admin SPA (scaffolded at M6)

React + Vite + Tailwind/Shadcn + TanStack, multi-store switcher, CRUD scaffolded
from the Drizzle schema. Consumes the same typed `hc` client as the storefront.

Placeholder until M6 (see ../../docs/BUILD-PLAN-RH-v1.md). Thin admin first
(orders + products/stock), then full.
